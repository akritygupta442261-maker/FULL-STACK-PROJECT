<!DOCTYPE html> 
<html lang="en"> 
<head> 
  <meta charset="utf-8" /> 
  <meta name="viewport" content="width=device-width, initial-scale=1" /> 
  <title>Edu Connect - Student Portal</title> 
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/fontawesome/6.4.0/css/all.min.css"> 
 
 <style> 
*{margin:0; padding:0; box-sizing:border-box; font-family:'Segoe UI',sansserif;} 
 
/* color variables */ 
:root{ 
  --bg: #f9f9f9; 
  --fg: #111827; 
  --primary: #004aad; 
  --accent: #ffd700; 
  --card-bg: #ffffff; 
  --muted: #4b5563; 
  --hero-text: #ffffff; 
  --header-text: #ffffff; 
  --footer-bg: var(--primary); 
} 
 
/* dark theme overrides */ 
[data-theme="dark"]{ 
  --bg: #0b1220; 
  --fg: #e6eef8; 
  --primary: #073766; 
  --accent: #f6c85f; 
  --card-bg: #071022; 
  --muted: #9aa6bd; 
  --hero-text: #ffffff; 
  --header-text: #ffffff; 
  --footer-bg: #073766; 
} 
 
body{   background:var(--bg);   color:var(--fg); 
} 
 
/* Header */ header{   background:var(--primary);   color:var(--header-text);   padding:15px 28px;   display:flex;   justify-content:space-between;   align-items:center;   gap:12px; 
} 
.brand { display:flex; align-items:center; gap:12px; } header h1{font-size:20px; margin:0; color:var(--header-text);} nav ul{list-style:none; display:flex; gap:16px; align-items:center;} nav ul li a{   color:var(--header-text);   text-decoration:none;   font-weight:500;   transition:0.2s; 
} 
nav ul li a:hover{ color:var(--accent); } 
 
.controls { display:flex; align-items:center; gap:10px; } 
 
/* Theme toggle */ 
#theme-toggle{ background:transparent; border:1px solid rgba(255,255,255,0.18);   color:var(--header-text);   padding:6px 10px;   border-radius:6px;   cursor:pointer; 
} 
 
/* Hero Section */ 
.hero{   background:linear-gradient(rgba(0,0,0,0.45),rgba(0,0,0,0.25)),              url("https://images.unsplash.com/photo-1529070538774-
1843cb3265df") no-repeat center/cover;   height:70vh;   display:flex;   flex-direction:column;   justify-content:center;   align-items:center;   text-align:center;   color:var(--hero-text);   padding: 0 20px; 
} 
.hero h2{font-size:40px; margin-bottom:12px; color:var(--hero-text);} .hero p{font-size:18px; max-width:640px; line-height:1.4; color:var(--herotext);} .btn{   margin-top:20px;   padding:12px 25px;   background:var(--accent);   color:#000;   border:none;   border-radius:6px;   cursor:pointer;   font-weight:bold;   transition:0.25s; 
} 
.btn:hover{ background:#fff; color:var(--primary); transform:translateY(2px); } 
 
/* Features */ 
.features{   display:grid;   grid-template-columns:repeat(auto-fit, minmax(250px,1fr));   gap:20px;   padding:44px;   max-width:1100px;   margin:0 auto; 
} 
.card{   background:var(--card-bg);   color:var(--fg);   padding:22px;   border-radius:12px;   box-shadow: 0 6px 18px rgba(2,6,23,0.12);   text-align:center;   transition:0.25s;   border: 1px solid rgba(0,0,0,0.03); 
} 
.card:hover{ transform:translateY(-6px); } 
.card i{ font-size:38px; color:var(--primary); margin-bottom:10px; } 
.card h3{ margin:10px 0; font-size:18px; } 
.card p{ color:var(--muted); } 
 
/* Footer */ footer{ 
  background:var(--footer-bg);   color:var(--header-text);   padding:18px;   text-align:center;   margin-top:30px; 
} 
 
/* responsive */ @media (max-width:700px){   header{padding:12px;}   nav ul{gap:10px; font-size:14px;} 
.hero h2{font-size:28px;} 
.features{padding:28px;} 
} 
nav a.active{ text-decoration:underline; text-underline-offset:6px; } 
</style> 
</head> 
 
<body data-page="home"> 
  <!-- Header --> 
  <header> 
    <div class="brand"> 
      <h1>Edu Connect</h1> 
    </div> 
 
    <div style="display:flex; align-items:center; gap:18px; width:100%; justify-content:space-between;"> 
      <nav aria-label="Main navigation"> 
        <ul> 
          <li><a href="index.html">Home</a></li> 
          <li><a href="educourse.html">Courses</a></li> 
          <li><a href="eduform.html">Register</a></li> 
          <li><a href="edulogin.html">Login</a></li> 
        </ul> 
      </nav> 
 
      <div class="controls"> 
        <button id="theme-toggle" aria-label="Toggle theme" ariapressed="false">🌙</button> 
        <a href="edulogin.html" style="color:#fff; text-decoration:none; fontweight:600;">Login</a> 
        <a href="#" id="logout" style="color:#fff; text-decoration:none; fontweight:600;">Logout</a> 
      </div> 
    </div> 
  </header> 
 
  <!-- Hero Section --> 
  <section class="hero"> 
    <h2>Welcome to Edu Connect</h2> 
    <p>Your one-stop student portal for courses, resources, and success. 
Sign up to join courses, track progress, and get support.</p> 
    <button class="btn">Get Started</button> 
  </section> 
 
  <!-- Features Section --> 
  <section class="features"> 
    <div class="card"> 
      <i class="fa-solid fa-book"></i> 
      <h3>Courses</h3> 
      <p>Explore various academic and skill-based courses tailored for you.</p> 
    </div> 
    <div class="card"> 
      <i class="fa-solid fa-user-plus"></i> 
      <h3>Student Registration</h3> 
      <p>Quick and easy registration to access all features of the portal.</p> 
    </div> 
    <div class="card"> 
      <i class="fa-solid fa-right-to-bracket"></i> 
      <h3>Login Portal</h3> 
      <p>Secure login for students and faculty to manage their accounts.</p> 
    </div> 
    <div class="card"> 
      <i class="fa-solid fa-envelope"></i> 
      <h3>Support</h3> 
      <p>24/7 help for students to solve queries and technical issues.</p> 
    </div> 
  </section> 
 
  <!-- Footer --> 
  <footer> 
    <p>© 2025 Edu Connect | Student Portal</p> 
  </footer> 
 
  <!-- Working Theme Script --> 
<script> 
(function(){ 
    const themeToggle = document.getElementById('theme-toggle'); 
 
    function applyTheme(theme) {       if (theme === 'dark') {         document.documentElement.setAttribute('data-theme', 'dark');         themeToggle.textContent = '☀️'; 
        themeToggle.setAttribute('aria-pressed', 'true'); 
      } else { 
        document.documentElement.removeAttribute('data-theme');         themeToggle.textContent = '🌙';         themeToggle.setAttribute('aria-pressed', 'false'); 
      } 
      localStorage.setItem('edu_theme', theme); 
    } 
 
    (function initTheme(){       let saved = localStorage.getItem('edu_theme');       if (saved === 'dark' || saved === 'light') {         applyTheme(saved);         return; 
      } 
      const prefersDark = window.matchMedia('(prefers-color-scheme: 
dark)').matches;       applyTheme(prefersDark ? 'dark' : 'light'); 
    })(); 
 
    themeToggle.addEventListener('click', (e) => {       e.preventDefault(); 
      const current = document.documentElement.getAttribute('datatheme') === 'dark' ? 'dark' : 'light'; 
      applyTheme(current === 'dark' ? 'light' : 'dark'); 
    }); 
  })(); 
  </script> 
</body> 
</html> 
 
 
